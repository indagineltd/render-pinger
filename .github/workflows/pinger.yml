name: Keep Site Awake

on:
  workflow_dispatch:       # 👈 allows manual triggering
  schedule:
    - cron: '*/1 * * * *'  # every 1 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your site
        run: curl -s https://www.tuttowebdev.com
